@model IEnumerable<Vendas.Models.PedidoVendaItem>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.vQuantidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.vValorUnitario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.vValorTotalProduto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.vQuantidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vValorUnitario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vValorTotalProduto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPedidoVendaItem }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPedidoVendaItem }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPedidoVendaItem })
        </td>
    </tr>
}

</table>
